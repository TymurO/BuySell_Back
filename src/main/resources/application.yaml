
spring:
  datasource:
    password: admin
    username: admin
    url: jdbc:postgresql://localhost:5432/buysell_angular
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true